'use client'

import { CreateWallet } from "@camino/auth";

const CreateWalletView = () => {
  return <div className="flex flex-col items-center justify-center h-full p-4">
  <CreateWallet />
</div>
};

export default CreateWalletView;
